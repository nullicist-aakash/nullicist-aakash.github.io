# nullicist-aakash.github.io
This github page contains the JSON corresponding to my resume in resume.json.

To generate `index.html`, run the following command:
```
resume export index.html --theme stackoverflow
```